{
  "name": "cotudes-practice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-manifest.js && vite build",
    "build:single": "node scripts/build-manifest.js && vite build --mode singlefile",
    "preview": "vite preview",
    "manifest": "node scripts/build-manifest.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "htm": "^3.1.1",
    "marked": "^15.0.0",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
